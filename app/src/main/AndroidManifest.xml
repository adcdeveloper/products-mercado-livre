<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mercadolivre">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".MercadoLivreApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".presentation.SplashActivity"
            android:screenOrientation="sensor"
            android:theme="@style/AppTheme.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.products.list.view.ProductsActivity"
            android:screenOrientation="sensor" />

        <activity
            android:name=".presentation.products.search.view.SearchActivity"
            android:screenOrientation="sensor" />

        <activity
            android:name=".presentation.products.detail.view.ProductDetailActivity"
            android:screenOrientation="sensor" />

    </application>

</manifest>